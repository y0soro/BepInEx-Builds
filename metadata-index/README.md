# Decrypted global-metadata.dat index

Requires patched BepInEx builds from https://github.com/y0soro/BepInEx-Builds to load.

No config required, will set internal metadata path to decrypted global-metadata.dat in `BepInEx/metadata-index` if game assembly hash matches.
