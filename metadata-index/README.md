# Decrypted global-metadata.dat index

Requires patched BepInEx builds from https://github.com/y0soro/BepInEx-Builds to load.

No config required, will fallback to searching decrypted global-metadata.dat in `BepInEx/metadata-index` if the original loading logic fails.
