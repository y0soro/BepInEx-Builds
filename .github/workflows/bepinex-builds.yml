name: bepinex-builds
on:
  workflow_dispatch:
  push:
    tags:
      - "*"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup dotnet
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - uses: awalsh128/cache-apt-pkgs-action@latest
        with:
          packages: mingw-w64
          version: 1.0

      - name: Setup xmake
        uses: xmake-io/github-action-setup-xmake@v1.2.5
        with:
          xmake-version: 3.0.8
          actions-cache-folder: .xmake-cache

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Build
        id: build
        uses: actions/github-script@v9
        with:
          script: |
            const script = require('./ci-build.cjs')
            return await script({ github, context, core, glob, io, exec, getOctokit, require })

      - uses: actions/upload-artifact@v7
        with:
          name: Builds.zip
          path: builddir/BepInEx/bin/dist/*.zip

      - name: Release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v3
        with:
          body_path: CHANGELOG.md
          files: builddir/BepInEx/bin/dist/BepInEx*-Unity.IL2CPP-win-x64-*.zip
